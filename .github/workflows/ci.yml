name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build (swift build)
    runs-on: macos-15
    # Skelf needs the macOS 26 SDK (Xcode 26+). Until GitHub-hosted runners ship it, this
    # job can't compile, so don't let it fail the whole workflow / show the repo as red.
    # Remove continue-on-error once a macos-26 / Xcode 26 runner image is available.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Select latest Xcode
        # Skelf targets macOS 26 / Liquid Glass, so it needs the macOS 26 SDK (Xcode 26+).
        # Once GitHub runners ship it, `latest-stable` picks it up.
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - name: swift build
        run: swift build -v

  lint:
    name: SwiftLint
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Install SwiftLint
        run: brew install swiftlint
      - name: Lint
        run: swiftlint lint --reporter github-actions-logging
